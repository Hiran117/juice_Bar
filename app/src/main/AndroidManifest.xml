<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.home">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/juice"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.UpdateReservation"></activity>
        <activity android:name=".activities.ReservationActivity" />
        <activity android:name=".activities.ReservationConfirmScreen" />
        <activity android:name=".activities.PickHallScreen" />
        <activity android:name=".activities.ReservationScreen" />
    </application>

</manifest>